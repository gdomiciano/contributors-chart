version: 2
jobs:
  build:
    machine: true
    steps:
     - checkout
     - run: npm run dcr:build
     - run: npm run dcr:run

#  test:
#    machine: true
#    steps:
#      - checkout
#      - run: npm run dcr:run

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
#      - test
